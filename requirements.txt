Django>=5.0,<6.1
Pillow>=10.0
python-dotenv>=1.0
dj-database-url>=2.1

# --- Below are only needed depending on where you deploy ---
# Render/Railway/Neon (Postgres): keep these
gunicorn>=21.2
whitenoise>=6.6
psycopg2-binary>=2.9

# PythonAnywhere (their free MySQL): install separately in a PA bash console
# instead of listing here, since mysqlclient needs system build tools that
# vary by OS and would break local Windows installs:
#   pip3.10 install --user mysqlclient
